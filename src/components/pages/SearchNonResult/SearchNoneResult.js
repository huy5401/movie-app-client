import React from 'react';
import classNames from 'classnames/bind';
import styles from './SearchNoneResult.module.scss';

const cx = classNames.bind(styles);
export default function SearchNoneResult({searchKey}) {
  return (
    <div>
        
    </div>
  )
}
